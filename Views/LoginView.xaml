<Window x:Class="DocMgt.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用户登录" Height="320" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="文档管理系统" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <StackPanel Margin="0,10">
                <TextBlock Text="手机号:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPhone" Height="30" Margin="0,0,0,15"
                         MaxLength="11"
                         PreviewTextInput="Phone_PreviewTextInput"/>

                <TextBlock Text="密码:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="30" Margin="0,0,0,20"/>

                <Button Content="登录" Height="35" Click="Login_Click"
                        Background="#007ACC" Foreground="White"/>

                <TextBlock x:Name="txtError" Foreground="Red"
                           HorizontalAlignment="Center" Margin="0,10,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
